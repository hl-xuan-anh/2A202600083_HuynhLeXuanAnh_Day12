events {}

http {
    upstream agents {
        least_conn;
        server agent:8000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://agents;
        }
    }
}